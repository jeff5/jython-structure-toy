/**
 * Example library package exported for safe access to lower-level
 * capabilities (requiring {@code MethodHandle} and using
 * {@code Throwable} to communicate exceptions).
 */
package example.runtime;
